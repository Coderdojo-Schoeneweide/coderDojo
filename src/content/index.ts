// Main content export - site-wide configuration only
// Page content is now managed via Markdown Content Collections in src/content/pages/
// Workshop content is managed via Markdown in src/content/workshops/

export { siteConfig } from './site'
export type { SiteConfig } from './site'
